#What is data structure?
# It is a way of oganizing and storing your your data
#  such that it can be used efficiently
# There are 4 types of data structure in python
# set {}, tuple (), list [] and dictionary {}

## Why do we need data structure
# In real world, you have multiple values, instead of one variable, you need data structure
# list [ 10, 20, 30] => common
# tuple (10, 20, 30) => No changes!
# set {10, 20, 30} => unique
# dictionary { 'a':10, 'b': 20, 'c': 30} key value pair
# functions nad methods
# function
costs = [10, 20, 30]
print(len(costs))
# Method
costs.remove(20)